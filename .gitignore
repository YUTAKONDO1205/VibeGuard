node_modules/
dist/
*.log
*.tsbuildinfo
.DS_Store
nul
coverage/
.env
.env.local
.vscode/settings.json

# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.idea/
*~

# AI coding assistants (local-only tooling / config / harness docs)
.claude/
.codex/
.cursor/
.windsurf/
.aider*
AGENTS.md
README-CODEX.md

# Security scan outputs
scan.json
report.sarif
report.md
gate.json
safe.json
vuln.json
diff.json
diff-report.md

# Release artifacts (built locally for manual publish)
vibeguard-chrome-*.zip
vibeguard-aicoding-*.vsix
vibeguard-cli-*.tgz

# Paper / research experiment data (local-only; outputs are regenerable,
# contain machine-specific paths and a stray nested git repo)
paper_data/
docs/EVALUATION.md

# Security red-team research workspace (local-only; kept out of the public
# service repo until mitigations + write-up ship — see the folder's scope doc)
security-experiment/
